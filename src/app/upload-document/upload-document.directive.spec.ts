import { UploadDocumentDirective } from './upload-document.directive';

describe('UploadDocumentDirective', () => {
  it('should create an instance', () => {
    const directive = new UploadDocumentDirective();
    expect(directive).toBeTruthy();
  });
});
